import * as Cortex from 'cortex';

export const user_context = new Cortex.Context({

});

user_context.addEventListener('login', event => {

});

user_context.addEventListener('logout', event => {

});